import { PRELOADER_SCENE } from "../../Utils/constants";

export class PreloaderScene extends Phaser.Scene {
    constructor() {
        super({
          key: PRELOADER_SCENE,
        });
      }
    
}